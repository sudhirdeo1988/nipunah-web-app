/**
 * API Configuration Constants
 */

/**
 * External API Base URL (default when process.env.API_BASE_URL is not set).
 * Server-side proxies under src/app/api/** may use API_BASE_URL from the environment instead.
 * Note: No trailing slash - add it when building URLs
 */
export const API_BASE_URL = "http://139.59.22.7:8080/api";
